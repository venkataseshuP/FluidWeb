import { APIRequestTemplatePK } from './apirequest-template-pk.model';

describe('APIRequestTemplatePK', () => {
  it('should create an instance', () => {
    expect(new APIRequestTemplatePK()).toBeTruthy();
  });
});
