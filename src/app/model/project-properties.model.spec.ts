import { ProjectProperties } from './project-properties.model';

describe('ProjectProperties', () => {
  it('should create an instance', () => {
    expect(new ProjectProperties()).toBeTruthy();
  });
});
