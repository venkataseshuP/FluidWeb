import { APIRequestTemplate } from './apirequest-template.model';

describe('APIRequestTemplate', () => {
  it('should create an instance', () => {
    expect(new APIRequestTemplate()).toBeTruthy();
  });
});
